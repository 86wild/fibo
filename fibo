const fibo = (n) => {
  if (n === 0 || n === 1) {
    return 1;}
return fibo(n-2)+fibo(n-1)
};

const fiboRow = (limit) => {
let x=0; 
do { 
  console.log(fibo(x));
  x++;}
while (fibo(x) <= limit)
}

console.log(fiboRow(100));
